# Safe Ingestion Engine — Python dependencies
# Pin major versions for reproducible builds

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.29.0
pydantic==2.12.5
pydantic-settings==2.2.1

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Task queue ────────────────────────────────────────────────────────────────
celery==5.3.6
redis==5.0.4

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.32.5
httpx==0.27.0

# ── Data / scraping ───────────────────────────────────────────────────────────
PyYAML==6.0.1
python-dotenv==1.0.1

# ── Dashboard ────────────────────────────────────────────────────────────────
streamlit==1.55.0
pandas==2.2.2

# ── PDF export ────────────────────────────────────────────────────────────────
reportlab==4.2.0

# ── PII detection (optional — Presidio NER) ──────────────────────────────────
# Uncomment to enable ML-based PII detection (requires ~500MB download)
# presidio-analyzer==2.2.354
# presidio-anonymizer==2.2.354
# spacy==3.7.4

# ── Database migrations ───────────────────────────────────────────────────────
alembic==1.13.1
sqlalchemy==2.0.30
